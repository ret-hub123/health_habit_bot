# Habit Guardian

Flask-based habit tracking app with SQLite.

## Quick Start

```bash
git clone https://github.com/ret-hub123/health_habit_bot.git
cd habit-tracker
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py